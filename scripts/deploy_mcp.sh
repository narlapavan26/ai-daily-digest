#!/usr/bin/env bash
# scripts/deploy_mcp.sh
# Purpose: Shell helper to package and deploy the mcp/ directory to Prefect Horizon (or push to deploy repo).
# Not implemented — add rsync/git push/curl steps and document required env vars when ready.
# On Windows, use Git Bash or WSL to run this script.
