"""
scripts/test_subgraph_local.py
==============================
Purpose:
  Run a single LangGraph subgraph (e.g. rss) in isolation with fixture or live MCP for debugging.

When implemented:
  - CLI: --source rss [--fixture path.json]; print SubgraphOutput JSON.

Notes:
  - Useful before wiring the full digest_graph.
"""
