# digest_runner dependencies
# Install with: pip install -r digest_runner/requirements.txt

# Core pipeline
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.27

# LLM enrichment (Instructor + provider clients)
instructor>=1.3.0          # Structured LLM output
openai>=1.30.0             # Groq uses OpenAI-compatible API; also used for Gemini via compat layer
google-generativeai>=0.7.0 # Gemini SDK (backup provider)

# LangGraph orchestration (for graph wiring — Priority 5+)
langgraph>=0.2.0
langchain-core>=0.2.0

# Supabase (for storage — Priority 6+)
supabase>=2.0.0

# Publishers (for notifications — Priority 7+)
python-telegram-bot>=20.0
resend>=2.0.0
requests>=2.31.0           # Discord webhooks via simple POST
