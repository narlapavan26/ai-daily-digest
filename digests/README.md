# digests/

Generated digest outputs (Option C strategy).

- `latest.md` / `latest.html` — always point to the most recent successful run.
- `YYYY-MM-DD/` — per-day archive with markdown, HTML, optional `digest_data.json` (FinalDigestSchema).

Populated by `digest_runner` and GitHub Actions after implementation.
